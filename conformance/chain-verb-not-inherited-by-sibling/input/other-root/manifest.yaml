# A root package genuinely unrelated to chain-grandparent's line: exists
# so chain-cousin-invalid-verb can prove KBF007's union is scoped to a
# package's actual ancestors, not to "everything passed to kbf lint in
# the same invocation". No ontology content of its own: an empty
# vocabulary is exactly the point.
name: chain-other-root
version: 0.1.0
spec: v0
extends: null
