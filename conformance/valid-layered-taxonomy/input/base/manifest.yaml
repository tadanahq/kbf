# layer: base, extending the root directly: KBF013 requires this to
# extend a root-or-base playbook (it does) and its name to start with
# core- (it does).
name: core-lending
version: 0.1.0
spec: v0
extends: core-library
layer: base
