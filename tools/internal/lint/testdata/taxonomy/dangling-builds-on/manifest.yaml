# builds-on names a playbook that was never passed to kbf lint: KBF011
# alone should speak to it. KBF013's builds-on-layer check has no parent
# to compare against and must stay silent rather than pile on a second,
# confusing finding about the same underlying gap.
name: core-tax-dangling
version: 0.1.0
spec: v0
builds-on: [tax-nonexistent-parent]
layer: core
