# layer: core without a core- prefixed name: KBF013's name-prefix rule.
# builds-on: [] is correct here on purpose, so this fixture isolates the
# name-prefix violation from the builds-on-shape one.
name: tax-noprefix
version: 0.1.0
spec: v0
builds-on: []
layer: core
