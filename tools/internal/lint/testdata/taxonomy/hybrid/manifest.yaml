# layer: vertical building on TWO core playbooks: the diamond/hybrid
# shape. Valid: vertical may build on any number of core-or-vertical
# playbooks, not just one.
name: tax-hybrid
version: 0.1.0
spec: v0
builds-on: [core-tax-root, core-tax-child]
layer: vertical
