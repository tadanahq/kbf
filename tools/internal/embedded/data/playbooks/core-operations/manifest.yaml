# DO NOT EDIT: generated from playbooks/core-operations/manifest.yaml by scripts/embedsync. Edit the source, then run `make embed-sync`.
# core-operations: the core playbook for businesses that operate from one
# or more physical or scheduled sites (retail, hospitality, field services).
# Adds location and shift on top of core-business; does not fork it.
name: core-operations
version: 0.1.0
spec: v0
builds-on: [core-business]
layer: core
