---
type: spec-tasks
status: in_progress
---
# kbf-v0-foundation – Tasks

## Batch 1: Scaffold + model + schema

- [x] Go module under `tools/` (module `github.com/tadanahq/kbf/tools`; two owner corrections, 2026-08-13: org `kozmo-hq`→`tadanahq`, then slug `kozmo-bf`→`kbf` to match the CLI/spec's actual name), cobra skeleton with `kbf` root + `schema` command — `go run ./cmd/kbf --help` lists `schema` (lint lands with Batch 2). Verified. Both renames applied across every `tools/` and `scripts/` file (imports, go.mod, schema `$id` URLs, license headers), schema regenerated, `grep -rn "kozmo-hq\|kozmo-bf" tools/ scripts/ schema/` clean; the on-disk repo folder (still `kozmo-bf`) is untouched, only code references changed.
  - go.mod created, deps fetched (cobra, goccy/go-yaml, invopop/jsonschema, lipgloss). design.md gained an "Implementation clarifications" section resolving tier vocab, KBF007/008 mechanics, extends resolution, schema scope — read that before touching lint or schemagen. Content agent (parallel) has since appended a relation-identity clarification (KBF003/008 key on (name,from,to) for relations) — binding for Batch 2 rules.
- [x] Canonical model structs in `internal/model/` for the seven primitives per design — 7 files (model.go + entity/relation/metric/action/competency/manifest/slots.go), every field has a why-comment. Compiles, gofmt clean.
- [x] `internal/schemagen/` + `kbf schema [--check]` generating `schema/ontology.schema.yaml` + `schema/manifest.schema.yaml` — Doc comments flow into schema `description` via invopop's AddGoComments (had to compute base/dir dynamically so it works from both repo root and tools/; see comment on `modelImportComponents`). Verified: generates clean YAML, `--check` exits 0 when current, exits 1 with a clear "stale" message after a struct-comment edit, exits 0 again after revert.
- [x] Makefile (`check` = fmt, lint, test, dogfood-lint, conformance, schema-freshness, boundaries) + GitHub Actions workflow mirroring it — root `Makefile` (fmt/lint cover both `tools/` and `scripts/`), `.github/workflows/ci.yml` runs the identical `make check` (installs golangci-lint v2.11.4, no separate lint step, so CI can't drift from local). `make check` is green end to end: dogfood-lint and boundaries do real work now (real content/scanner exist), only `conformance` still degrades to a skip message (no fixtures yet, Batch 4).
- [x] Apache-2.0 LICENSE, .gitignore finalized — Root `LICENSE` (canonical unmodified text). Per CONTRIBUTING.md's already-written policy ("Go source files under tools/ carry the standard Apache-2.0 header comment"), added the short-form header to all Go files in both `tools/` and `scripts/`, verified it doesn't leak into `go doc` output (blank line before the package doc comment) and doesn't break `AddGoComments`/schema-freshness. `.gitignore` extended (editor dirs, coverage.out) with comments grouping the existing entries.

## Batch 2: Linter

- [x] Position-aware loader (goccy) + manifest/extends resolution — `internal/lint/loader.go`: AST-walking decode (not DisallowUnknownField) so every unknown field is reported in one pass, each at its own line; malformed YAML/missing manifest degrade to KBF-coded findings, never a panic or aborted run. `extends` resolves by manifest name across every path given to `kbf lint`, not by filesystem convention (design.md). Verified: `kbf lint ../packages/universal-core ../examples/cafe-demo` is clean; either alone against a bad/missing path degrades correctly.
- [x] Structural validation against model (KBF001-KBF004, KBF010-KBF011) — `internal/lint/structural.go` + `identity.go`. Found and fixed two real gaps while dogfooding against packages/universal-core and examples/cafe-demo (both recorded in design.md): (1) KBF002 was designed but never implemented for tier/cardinality/additivity/risk values, only for unknown `kind`; a failing unit test caught it, real content didn't (it was all valid). (2) universal-core legitimately uses `cardinality: many-to-one` 7 times (every child-to-parent relation); the original 3-value enum was genuinely incomplete, not a content mistake — fixed in `internal/model.Cardinalities`, schema regenerated. `spec/primitives/relation.md`'s field table needs the same fix; flagged for the content batch, not made here (out of tree scope).
- [x] Semantic rules (KBF005-KBF009, KBF012) — `internal/lint/semantic.go`. KBF012's real direction (a slots.yaml row with no matching attribute, not the reverse) came from reading `spec/primitives/slot-mapping.md`, which the content agent had already written correctly; my first mental model had it backwards, caught before writing code. KBF007's vocabulary and KBF008's fork/override matching both key relations on (name,from,to) per the content agent's design.md addendum. Verified end to end against both real packages together (clean) and each alone (correct cascading KBF011→006/007/009/012 degradation, e.g. cafe-demo alone: 36 findings, all traceable to the missing parent).
- [x] Renderers: lipgloss human + stable `--format json` — `render_human.go` (one lipgloss table per file) + `render_json.go`. Golden tests force `termenv.Ascii` in the test binary only (the real CLI keeps normal terminal color auto-detection). Fixed JSON's default HTML-escaping, which was turning fix hints' `<placeholder>` syntax into `<placeholder>` — a real, visible defect in the stable agent-facing interface, not a cosmetic nitpick.

Unit tests: `internal/lint/{structural,semantic,render}_test.go`, 15 tests (all 12 rules + a clean-package baseline + 2 golden renderer cases x2 fixtures), fixtures under `internal/lint/testdata/` (generic names — widget/gizmo/workshop — not Demo Cafe, kept out of `packages/`/`examples/`). `go test ./...` green.

**Scope note for the orchestrator**: `kbf coverage` and `kbf compile --to mermaid` (tasks.md Batch 3) are Go code, not content, but this dispatch's instructions were explicit — "Do NOT do Batch 3 or 4" — so they are untouched. The content agent's own note on those two bullets ("tooling agent; outside this agent's scope") means both sides agree they need a Go-writing dispatch; nobody currently owns them. The README tour (Batch 4, content-complete) and this capsule's `kbf coverage`/`kbf compile` done-means both depend on them landing.

## Batch 3: Content

- [x] `packages/universal-core` per design (entities, verbs, metrics, actions, questions, manifest, slots) — Content complete: manifest (`extends: null`), 9 entity files (organization, location, order, product, customer, employee, shift, supplier, purchase — each meaning/identity/resolution/tier/synonyms(en+es)/attributes; states omitted on customer and supplier to keep it visibly optional), `ontology/relations.yaml` (15 relations, all 12 controlled-vocabulary verbs present at least once per the KBF007 clarification, 3 verbs recur across distinct (name,from,to) pairs, see the new design.md note on relation identity), `ontology/metrics.yaml` (6 metrics, grain+additivity+unit+tier on all, thresholds on labor-cost-ratio and gross-margin), `ontology/actions.yaml` (4 actions with risk auto/confirm, no tier field per the clarification), `evals/competency-questions.yaml` (9, one per entity, `expects` avoids bare-verb ambiguity where a verb recurs), `install/slots.yaml` (26 template rows, one per declared attribute, empty sources). Done-means confirmed for real: `kbf lint packages/universal-core` is green (Batch 2 landed).
- [x] `examples/cafe-demo` per design — Content complete: manifest (`extends: universal-core`), `ontology/entities.yaml` (product synonym override, minimal fragment), `ontology/relations.yaml` (new client-configured self-relation `location belongs-to location`), `ontology/metrics.yaml` (labor-cost-ratio threshold-only override + net-new waste-ratio), `evals/competency-questions.yaml`, `install/slots.yaml` (all 26 slots, `demopos`/`demobooks`, 3 `crm.customer-*` deliberately unmapped). Done-means confirmed for real: `kbf lint examples/cafe-demo packages/universal-core` is green; `kbf coverage` (below) confirms exactly the 3 crm.customer-* rows unmapped, 23/26 mapped.
- [x] `kbf coverage` command — `internal/coverage/` (coverage.go + render.go), `cmd/kbf/coverage.go`. Builds on `lint.Load`/`lint.Universe` (refactored `internal/lint/universe.go` to expose these, no loading logic duplicated). Reports only on **leaf** packages in the given set (`Universe.IsLeaf`: not the extends-root of any other loaded package) — universal-core given alongside cafe-demo is resolution context only, not a second, noisy all-unmapped report; linted alone it's trivially its own leaf and reports normally. Golden-tested (`internal/coverage/coverage_test.go`) against the real packages: confirms exactly 23/26 mapped, the 3 crm.customer-* rows unmapped, matches the coordinator's spec exactly.
- [ ] `kbf compile --to mermaid` — in progress.

## Batch 4: Conformance + boundaries + docs

- [ ] Conformance fixtures (≥6 valid, ≥6 invalid with expected rule ids) + thin runner — Done means: runner green; breaking a fixture fails with the named rule.
- [x] `scripts/boundaries.go` + make target + self-test — Stretch goal, done ahead of schedule (this batch's brief invited it: "implement boundaries scan + self-test if time allows"). Own Go module (`scripts/go.mod`): a blocklist-plus-heuristics scanner (private filesystem paths, prices), `make boundaries` runs the self-test then the real scan against the repo. **Important design call, not in the original brief**: the production blocklist ships *empty*. `scripts/boundaries.go` is itself published to the public repo it scans, so writing a real client/internal-project name into it as a literal string would be the exact leak this tool exists to catch, the moment it's committed — a blocklist that documents what must stay private, in public, defeats itself. `scan()` takes the blocklist as a parameter specifically so the self-test can exercise the real mechanism with synthetic terms (`Example-Confidential-Co`) without the production list needing real entries to do it. The repo owner should add real terms directly, each with a comment on why, in a commit they review themselves — not something an agent should guess at from incidentally-visible context. Two bugs found and fixed while building this: (1) `go build ./...` on a package-main module with no `-o` silently writes an executable named after the directory into that directory (`scripts/scripts`), which then got scanned as binary garbage — fixed by adding a proper NUL-byte binary sniff (same technique `git`/`grep -I` use) rather than trusting extensions alone, and by gitignoring build output; (2) the self-test's own synthetic fixture strings (an example absolute path shaped like a personal machine's home directory, an example dollar amount) tripped the real repo-wide scan since they live in a committed file — excluded `scripts/boundaries_test.go` specifically, by identity, everything else (including every *other* test file) stays fully in scope; this very sentence hit the same trap once by naming the two literal example strings instead of describing their shape, caught immediately by `make boundaries` itself failing on its own capsule note. 7 tests, all green; `TestRealRepoIsClean` runs the production scan inside `go test` too, so it fails in the same place as everything else.
- [-] Spec prose: `spec/index.md`, `spec/primitives/*.md` (7), `spec/package-format.md`, `spec/conventions.md`, `spec/versioning.md` — Content complete, 10 files. index.md frames the 7 primitives as semantic/action/operational elements with a reading order; each primitives doc has purpose, a fields table, one worked YAML example, and a common-mistakes list tied to real rule ids; package-format.md documents the manifest + folder anatomy including the reserved-not-linted `workflows/agents/surfaces`; conventions.md carries the full controlled-verb table (all 12, meaning + which recur), the kind-specific tier table, and the glossary-eligible-field table; versioning.md covers spec-v0.x tags, tool independence, migration-note policy, and names every field v0 leaves opaque and why. All frontmatter is `type: spec-doc`, one required key. Hand-verified: every YAML block across all 11 files (12 blocks total) parses; every canonical example programmatically diffed byte-for-byte against the real file in packages/ or examples/ it claims to be copied from (all match); the two illustrative "wrong" fork examples were removed from entity.md/metric.md after checking a future AST-based doc-extraction test would find them regardless of list-item indentation and could flag content that is deliberately invalid, so only real, lintable fragments remain in every yaml-tagged block. Done-means (doc-extraction test) is the tooling agent's to build; content is ready for it.
- [-] README (thesis + 5-minute tour) + CONTRIBUTING + rfcs/README — Content complete. README: the semantic-layers-vs-KBF-Ontology thesis up top, the evidence line in the honest/rounded phrasing already vetted in project-overview.md (no internal citations, no exact figures), a repo-layout table, a v0/config-phase-only status note, and a 7-step tour (clone, build via `cd tools && go build -o ../bin/kbf ./cmd/kbf`, lint universal-core, lint cafe-demo passing both paths since extends resolves against supplied paths not folder convention, coverage showing the deliberate unmapped crm.* slots, compile --to mermaid, and a break-it-on-purpose step demonstrating a KBF004 file:line error). Build command verified against the real `tools/cmd/kbf/main.go` path and cross-checked root.go's own v0/config-phase description for tone match. CONTRIBUTING.md: capsule-workflow summary pointing to AGENTS.md as canonical, the license-header split (Go files get a header, YAML/Markdown content doesn't, both covered by root LICENSE), RFC pointer. rfcs/README.md: when an RFC is needed (primitive shape, verb vocabulary, lint rule, package-format/versioning policy changes) vs. not (typos, clarifications), a 3-step issue-based process, explicitly no numbering scheme yet. Done-means ("tour commands copy-paste clean on a fresh clone") needs Batch 1 (Makefile/LICENSE), Batch 2 (lint), and Batch 3's remaining `coverage`/`compile` commands to all land first; not runnable yet, but every command in the tour is written against the real, already-built `tools/cmd/kbf` entrypoint.
