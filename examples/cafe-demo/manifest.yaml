# Demo Cafe: a fictional single-location cafe. Teaching content only,
# fully valid, exercising every KBF primitive at least once (directly or
# through composition). Builds on core-operations, which builds on
# core-business (`kbf lint`/`coverage`/`compile` need all three paths).
name: cafe-demo
version: 0.1.0
spec: v0
builds-on: [core-operations]
layer: vertical
