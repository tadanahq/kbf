# Demo Cafe: a fictional single-location cafe. Teaching content only,
# fully valid, exercising every KBF primitive at least once (directly or
# by inheritance through `extends`). Chain: cafe-demo -> operations-core
# -> universal-core (`kbf lint`/`coverage`/`compile` need all three paths).
name: cafe-demo
version: 0.1.0
spec: v0
extends: operations-core
